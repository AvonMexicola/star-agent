# Current content in the shared development build

Cees requests the new expedition character and other new content integrated into
the local development build for review. Start from the existing combined runtime
at `afe0654`, keep the ship/location launcher and persistent local account store,
and resolve incoming features semantically in an isolated integration worktree.

Inventory source commits, working candidates, validation and remaining limitations.
Freeze the explicitly requested, tested character candidate in a separate source
checkpoint without modifying its owner's branch/index or taking unfinished unrelated
files. Integrate ready audio, retail and construction checkpoints and current asset
review records. Read the live handoff before further snapshots or shared merges;
active rover, fauna, construction expansion and flight owners retain their lanes.

Expose integrated studios and review options through the local launcher. Assets
that remain studio-only or features that remain offline must say so. All playable
entries need keyboard/controller reach. Check the new character in real play and
remote presentation, preserving the current input, navigation, inventory, damage,
server authority and camera-origin contracts. Verify the combined build and review
routes with appropriate unit, server and production-browser checks.

Publish the verified result to local `dev/all-features`, preserve live handoff
edits, refresh the existing 5178 preview and restart its owned service only if
server changes require it. Keep PostgreSQL data intact. Update the source inventory,
evidence, remaining queue and handoff. This request does not publish main or a site.
