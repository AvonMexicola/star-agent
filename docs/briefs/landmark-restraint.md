# Quieter, rarer Aeon landmarks

Cees finds the accepted shapes too numerous from about 1 km and the latest
weathering excessive; reported frame rate may also coincide with other agents'
game tests. Thin the canonical landmark population by about 80%, retaining a
deterministic subset of the same v1 positions, shapes, sizes and IDs. Keep real
overhangs, collision parity, worker fallback, LOD fades and log depth intact.

Calm the material and remove unnecessary fragment work: one scanned colour and
normal scale, two broad mineral fields, restrained contrast and relief. Skip
normal reads past the relief fade and discard LOD-rejected fragments before
material sampling. Reuse the same shared CC0 maps with no new bitmap/dependency.

Base: local dev `219a584`. Worktree `star-agent-rock-restraint`, branch
`art/landmark-restraint`. Own landmark distribution/material, affected fixtures,
invariants and this task's docs. Preview port 5383, single browser worker after
the shared GPU queue. Record competing browser jobs, but do not terminate other
owners' jobs or claim they explain all observed slow frames.

Check unchanged retained transforms, reduced broad-region density, plant and
authoritative collision agreement, production build and actual browser shader
compilation. Capture identical before/after 1 km and close views, plus LOD/relief
transitions. Record browser/backend/scale and bounded warm frame observations;
shared desktop activity cannot establish exclusive-GPU performance acceptance.
This changes scenery population and shading, not controls or a playable route.

Population revision 2 needs client/worker reload and an API refresh together for
shared collision. No database/schema/save mutation. Preserve existing unrelated
work during the standing local integration; final independent acceptance and
public deployment remain separate.
