Read-only functional seam review complete: one P2 finding was identified and closed. No remaining blocker was found in this bounded source review.

Compared fleet `5f63893e9d88184696ad897ec664e3a2a94b9951` with medium integration `9cb0c4e5dfcf23b3752055fccff32edad0a02078`, then reviewed the root-owned Fleet correction in the working tree (exact main hash below). No production files were edited and no browser/GPU or broad suite was run. Root reported 1,057 normal tests and 187 multiplayer tests passing, with two explicit SQL skips; this report does not represent those as independently rerun.

The original P2 was in `src/main.js`'s Fleet callback: it committed the Gannet hull before awaiting `rover.spawn()`, ignored `false`, and promised that the former ship remained selected if placement threw. The actual source-extracted callback reproduced both false statements on original main SHA `966e34c30dd679fe6a0043f09b099008c893999623855de0d99732b0c358582d`. Root now awaits Burrow model readiness before committing selection. After a committed selection, placement failure is accurately reported with the Gannet still selected; it does not attempt to reconstruct earlier mechanism state through a synthetic rollback.

Closure uses the actual changed callback with injected dependencies, not a rewritten implementation. All six cases passed: ready/successful placement, null vehicle intake, rejected vehicle intake, rejected hull intake, false placement, rejected placement. Preparation failures retain Nomad and never spawn a rover. Placement failures retain Gannet and give an accurate retry message. The false-placement case additionally selects Nomad and then Gannet through the same callback and succeeds after placement becomes available. This proves callback/error semantics; it is not native gameplay or physical placement acceptance. [Original failure receipt](/tmp/star-agent-medium-functional-review/fleet-failure.json), [closure probe](/tmp/star-agent-medium-functional-review/fleet-closure.mjs), [closure receipt](/tmp/star-agent-medium-functional-review/fleet-closure.json).

Reviewed preserved contracts:

- **Shared authority and HUB:** protocol remains version5. Protocol, room, HUB service/policy, Atlas physical modules and tractor authority modules in the hash receipt are byte-identical to the checked fleet baseline (11 files). Server `cargoHull` still accepts only Nomad/Atlas; joining loads/configures Nomad before connection. Medium Fleet selection and local hull registration reject an active connection, ship cutters reject online context, and the existing `mining.onMine` wrapper returns without making local cuts online. No medium hull, ore field or alternative authority was added to the shared snapshot. HUB hands-free selection/holstering and its physical-neutral fire gate remain in the handheld tool; the two changed budget guards do not remove those checks.
- **Atlas meadow and ground access:** Atlas meadow still creates the rover, uses its established world seed/start path and delegates to `placeAtlasMeadow`. The shared `updateAtlasRampGround` hook is still called before the occupied-vehicle early return; it retains the Atlas-only guard, server snapshot rule, complete ramp-width ground sampling and parked-pose cache. Carrier profiles add Gannet behavior while retaining the existing Atlas aft-ramp and crew-lift paths and their composed rover obstruction guards.
- **Dedicated ore, supplies and freight:** `stratum-ore` has a fixed384kg resource allowance and zero supply allowance, separate from the shared `ship` supply manifest and SBU freight grids. Existing containers/hull crate records are retained by idempotent registration. A mining worker job captures its destination before dispatch; later tool or hull selection cannot redirect the accepted result. Rock/collider publication follows the same revision-checked atomic MiningStore commit. Local crate loading updates resource quantity, freight ledger and credits in one `store.write`; a failed write does not publish its candidate state. Registration of an empty bin and a hull are separate writes, but no ore/crate transfer is split across them.
- **Input and physical power synchronization:** the existing single Gamepad poll still feeds the shared controller callback. Modal/focus/device suppression is retained. Canvas drag now tracks one pointer and clears on up/cancel/lost capture, blur, visibility, pointer-lock and dialog-open/close mutation. Medium touch controls check focus, document visibility, modal state and powered flight before inserting movement keys, and clear held keys when context changes. Navigation publishes Gannet power before the rover's occupied-vehicle step; both that step and ordinary on-foot mechanism updates require main power. Stratum's ramp clock is called through that existing powered navigation gate. No second floor or new teleport was introduced by these seams.

Limits: this is not acceptance of new art, GPU behavior, actual touch/controller journeys, brightness, performance, or all asynchronous races. I authored the pending lamp helper/wrapper, Stratum browser fixtures and the earlier ship-mining core; they receive no independent self-approval here. The mining core was read only to trace the main/store/input boundaries. The lamp implementation is explicitly outside this review's approval scope.

Exact reviewed primary source SHA256 values (main is the corrected working-tree version; the other five remain9cb0c4e):

| Source | SHA256 |
|---|---|
| `src/main.js` | `a7b2753072677754dac16ad6ad22df7d4da25c3005f9ec70f2d84bccd3213b84` |
| `src/navigation.js` | `dbdb4e62c7400416cbc9f63f027d16447f8cc21aff7c74730b5e97bfc2ee2efa` |
| `src/nomad-cabin-controls.js` | `857752d9b832f720ea107ffea9f2033188a75dfbcd48353cbbeb20061ae104af` |
| `src/mining/tool.js` | `67c587278dce844f13341ef73bc1ea5e3db3703ce4af86f837cdd82f6e3deee2` |
| `src/trading/local.js` | `be58a0556d49cb0c479c6c7e05bc13ade83771679dcfce9aaa6cfae30bd3d034` |
| `src/trading/system.js` | `389a703c62009de2998bd72b3d4217e8b2216334bb262bce905df15a927d3db9` |

[Full source identities, related modules and baseline equality checks](/tmp/star-agent-medium-functional-review/source-hashes.json). The original failure evidence remains unchanged. Reproduce the bounded closure with `node /tmp/star-agent-medium-functional-review/fleet-closure.mjs` against the pinned corrected main.
