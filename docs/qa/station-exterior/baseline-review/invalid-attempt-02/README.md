INVALID CAMERA FIXTURES. This attempt on port 5400 completed without browser diagnostics, but visual inspection invalidated its apparent pass. The asynchronous dev transit overwrote the first camera and restored flight/cockpit mode. The ship/MFD geometry occludes the lower station in the later overviews. The first actual camera was millions of metres away from the requested station-relative eye. A projected bounding box alone did not catch that error. None of these images or counts are used for the accepted baseline/art score. The corrected paired run waits for transit completion and checks actual camera, first-person mode and hidden ship before every capture.
