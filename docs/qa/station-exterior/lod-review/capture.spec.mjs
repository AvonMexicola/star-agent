import {test,expect} from '/home/cees/projects/star-agent-station-exterior/node_modules/@playwright/test/index.mjs';
import fs from 'node:fs/promises';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import crypto from 'node:crypto';
const BASE=path.dirname(fileURLToPath(import.meta.url));
test('independent final distant exterior overview review',async({page,browser})=>{
 for(const variant of ['authored']){
 const OUT=BASE;await fs.mkdir(OUT,{recursive:true});
 const report={variant,browser:browser.version(),timestampUTC:new Date().toISOString(),console:[],captures:[],served:{},scope:'Scoped independent final-LOD overview follow-up. Two exact prior cameras with verified actual LOD rendering. Original art scores and materials-pending disposition remain unchanged. No FPS, gameplay or final acceptance claim.'};
 page.on('pageerror',e=>report.console.push({type:'pageerror',text:String(e)}));page.on('console',m=>{if(['error','warning'].includes(m.type()))report.console.push({type:m.type(),text:m.text()});});
 try{
  for(const url of ['/models/station.glb','/models/station_lod1.glb',...(variant==='authored'?['/models/station-exterior.glb','/models/station-exterior-lod1.glb']:[])]){const r=await page.request.get(url);expect(r.ok()).toBe(true);const bytes=await r.body();report.served[url]={bytes:bytes.length,sha256:crypto.createHash('sha256').update(bytes).digest('hex')};const expected={'/models/station-exterior.glb':'5b39b183030d529a710de0b2dad308a36a8e597b067d3061d82f01bb721b76e6','/models/station-exterior-lod1.glb':'ec98e225e57bbadb18c4c4567614bab2c5a23694e16effeeba99a4ce0b5ffdcb'};if(expected[url])expect(report.served[url].sha256).toBe(expected[url]);}
  await page.goto('/?dev=1&start=orbit&ship=nomad&intro=0&debug=1&seed=7291'+(variant==='authored'?'&stationExterior=1':''));await page.waitForFunction(()=>window.starAgent?.state.ready&&window.starAgent.state.station.ready&&window.starAgent.state.station.finish==='ready'&&!window.starAgent.state.transiting&&window.starAgent.state.dev?.ready,null,{timeout:90000});
  await page.evaluate(()=>{const a=window.starAgent,n=a.navigation,s=n.station;a.openingSequence?.leave();a.setRenderScale(1);n.enabled=false;n.keys.clear();n.velocity.set(0,0,0);n.angularVelocity.set(0,0,0);n.mode='walk';n.insideShip=false;n.dockedAtStation=false;n.cabinFlight=false;n.shipPosition=null;n.spaceParked=false;n.autoland=false;document.body.classList.remove('player-active');document.querySelectorAll('body > :not(canvas):not(script)').forEach(e=>e.style.visibility='hidden');if(!s.exterior.lod)throw Error('Distant exterior did not load');window.__stationReviewDraws={hero:0,lod1:0};s.exterior.group.traverse(m=>{if(!m.isMesh)return;let q=m,isLod=false;while(q){if(q===s.exterior.lod.group)isLod=true;q=q.parent;}const old=m.onBeforeRender;m.onBeforeRender=function(...args){window.__stationBaselineCamera=args[2];window.__stationReviewDraws[isLod?'lod1':'hero']++;return old.apply(this,args);};});});
  async function pose(eye,target){await page.evaluate(({eye,target})=>{const n=window.starAgent.navigation,s=n.station;window.__stationReviewDraws={hero:0,lod1:0};n.enabled=false;n.mode='walk';n.shipPosition=null;n.cabinFlight=false;n.position.fromArray(eye).applyQuaternion(s.baseQuaternion).add(s.centre);const t=n.position.clone().fromArray(target).applyQuaternion(s.baseQuaternion).add(s.centre);n.orientToward(t,s.up);s.exterior.rings.forEach(r=>r.rotation.x=0);}, {eye,target});await page.waitForTimeout(400);await page.waitForFunction(eye=>{const a=window.starAgent,s=a.navigation.station,st=a.state,v=a.navigation.position.clone().fromArray(st.camera.position).sub(s.centre).applyQuaternion(s.baseQuaternion.clone().invert());return st.mode==='walk'&&st.camera.mode==='first-person'&&!st.camera.shipVisible&&eye.every((x,i)=>Math.abs(x-v.toArray()[i])<.0001);},eye,{timeout:5000});}
  async function capture(name,eye,target,overview){
   await pose(eye,target);
   if(!overview)await page.waitForTimeout(2500);
   await page.screenshot({path:path.join(OUT,name+'.png')});
   const state=await page.evaluate(()=>{const a=window.starAgent,n=a.navigation,s=n.station,c=window.__stationBaselineCamera,gl=document.querySelector('canvas').getContext('webgl2'),e=gl.getExtension('WEBGL_debug_renderer_info'),st=a.state;const inv=s.baseQuaternion.clone().invert(),actualLocal=n.position.clone().fromArray(st.camera.position).sub(s.centre).applyQuaternion(inv);const projected=[];for(const x of [-1350,1350])for(const y of [-1500,1500])for(const z of [-1500,1500]){const v=s.centre.clone().set(x,y,z).applyMatrix4(s.exterior.group.matrixWorld).project(c);projected.push(v.toArray());}return {lodProof:{level:s.exterior.detailLevel,lodGroupVisible:s.exterior.lod.group.visible,heroFixedVisible:s.exterior.group.getObjectByName('FixedStructure').visible,heroRingVisibility:s.exterior.rings.map(r=>r.visible),lodRingVisibility:s.exterior.lod.rings.map(r=>r.visible),onBeforeRenderCalls:{...window.__stationReviewDraws}},mode:st.mode,shipVisible:st.camera.shipVisible,camera:{world:st.camera.position,complexLocal:actualLocal.toArray(),quaternion:c.quaternion.toArray(),navigationQuaternion:n.orientation.toArray(),fov:c.fov,aspect:c.aspect,mode:st.camera.mode},complex:{centre:s.centre.toArray(),quaternion:s.baseQuaternion.toArray(),ringRotationX:s.exterior.rings.map(r=>r.rotation.x)},backend:e?gl.getParameter(e.UNMASKED_RENDERER_WEBGL):gl.getParameter(gl.RENDERER),renderScale:st.renderScale,drawingBuffer:[gl.drawingBufferWidth,gl.drawingBufferHeight],drawCalls:st.drawCalls,triangles:st.triangles,station:st.station,heroPods:s.pods.filter(p=>p.model.visible).map(p=>p.id),lodBatches:s.lodBatches.length,projectedConservativeStationEnvelope:projected};});
   expect(state.station.exteriorDetail).toBe('lod1');expect(state.lodProof.lodGroupVisible).toBe(true);expect(state.lodProof.heroFixedVisible).toBe(false);expect(state.lodProof.heroRingVisibility).toEqual([false,false]);expect(state.lodProof.onBeforeRenderCalls.lod1).toBeGreaterThan(0);expect(state.lodProof.onBeforeRenderCalls.hero).toBe(0);
   report.captures.push({name,eyeComplexLocal:eye,targetComplexLocal:target,wholeStation:overview,cameraFixtureVerified:true,...state});
   if(overview)expect(state.projectedConservativeStationEnvelope.every(p=>Math.abs(p[0])<=.90&&Math.abs(p[1])<=.90&&p[2]>-1&&p[2]<1)).toBe(true);
  }
  await capture('overview-quarter',[-4484,2484.3,-4956],[0,-35,0],true);
  await capture('overview-ring-face',[-5600,1100,-1800],[0,-35,0],true);
  expect(await page.evaluate(()=>window.starAgent.state.station.exterior)).toBe(variant==='authored'?'geometry-review':'legacy');
 }finally{await fs.writeFile(path.join(OUT,'evidence.json'),JSON.stringify(report,null,2)+'\n');}
 expect(report.console).toEqual([]);
 }
});
