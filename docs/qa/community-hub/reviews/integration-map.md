# HUB / tractor / fleet delivery integration map

Read-only source audit. Frozen inputs: HUB **`004979d736bc5bfb39133622f35d0e0afce829f0`** in `/home/cees/projects/star-agent-community-hub`. Tractor/handheld delivery is **`7c4f568e89676d4686943ad010aa05964dae604f`** in `/home/cees/projects/star-agent-tool-art`. Their merge base is `3ed204b04a45e6c867241e0d91ece019fd0bf97d`. Fleet consequences below come from the current `star-agent-dev/HANDOFF.md` claim at lines3284–3289; its future implementation was not audited. No source, database, service, GPU or Git merge was changed/run.

**Do not use either branch's runtime files as wholesale replacements.** Sixteen paths changed on both sides. Both `src/multiplayer/protocol.js` files are identical and declare version4, while their application contracts differ. A clean textual hunk or a successful version4 handshake therefore does not prove that both features survived. Integrate against the checked tractor/PERF delivery, preserve the HUB authority semantics below, then coordinate the final client/server contract with the fleet steward's reserved protocol5.

## Files and hooks that require a semantic union

| Overlap | Preserve HUB | Preserve tractor / PERF; required reconciliation |
| --- | --- | --- |
| `server/room.js` | `createStationHub`, `createStationSecurity`, ramming resolver; `stationHub` requests; `hub.input/tick/update/action/canFire`; defense-pending/health gates; deferred shot damage through `security.submit`; `security.settle` on respawn/departure/close; per-player publication promises during inventory changes. | `publicState()` once per synchronous broadcast, private `state(p,shared)` per recipient; cached doorway scratch geometry; `p.nav.carryingCargo` includes leased tractor crates; suppress empty tractor-move notices. Keep HUB/defense hooks around the optimized tick rather than replacing it. Do not share private inventory, commerce account, health, nearby drops or passenger state between recipients. |
| `server/trading.js` | Finite market mapping, exact terminal reach/owned parked-ship checks; `normalizeCommerce`; transaction-local `ledger`; publish `state=result.state` only after `transactCommerce` completes. | Physical ships/loose crates, tractor context, loose collisions and getter-backed carrying state. Build tractor context from the transaction's ledger, not by restoring the old `state=current` publication inside the transaction callback. Keep the new market's stricter berth/ownership checks. |
| `src/trading/model.js` | `marketVersion:1`, `markets`, one-time legacy normalization, finite stock and integrated marginal prices. | `validLooseCargo`, all loose crates in the same global ID-uniqueness set, five `tractor-*` operations, active-lease hand-carry exclusion and rejection of old remote `haul`. Preserve both ledger extensions; neither validator alone validates the other's state. Both ledgers still call themselves version1. Never reset stock, loose crates, receipts, accounts or ship-grid cells when combining them. |
| `src/trading/system.js`, `src/trading/ui.js` | Hub commodity terminals, `markets/marketTerminals` snapshots and stock-aware quotes, correct ship-still-parked logic while the pilot visits the hub. | `createCargoTractor`, `getMuzzle`, tractor equip/controller/update/dispose, loose models and collision chains, secure-grid interaction. Keep both UI capabilities; physical cargo and finite market settlement share the same authoritative revision/idempotent receipt path. |
| `src/main.js`, `src/controller-hints.js` | `StationDefense` construction/readiness/update/state and `stationStrike` presentation; station services' online request adapter; loadout's `canSelect` guard; hands-free hints and elevator modal ownership. | Tractor muzzle/controller hooks and hints, plus current wildlife/performance additions. Compose the hint conditions, cached hint key and `loadout.item` override; neither branch's one-line replacement is a complete result. |
| `src/mining/tool.js` | Physical pointer/key/RT release gate across hub/transit, hub selection checks, stowing and hidden tool/effect state. | Tractor equipment model/calibration, `onTractorHolster`, tool changes, cargo-cabin use, no weapon/mining fire while tractor active. **New integration seam:** tractor `live()`/`tractorContext.active()` currently have no hub check. Apply hands-free/transit restrictions to tractor equip/render/grab/move/stow as appropriate; release held leases safely and require genuine neutral before relocking. Server checks must back the UI. |
| `src/multiplayer/client.js` | Hub/defense initial state, welcome/state publication, station/event forwarding and disconnect reset; avoid local `cargoAction` while `stationHubTransit` is active. | Allocation-free vector/quaternion reconciliation and `carryingCargo || tractorActive` fire suppression. Preserve both lifecycle paths, not only initial snapshots. |
| `src/multiplayer/ui.js` | Hands-free summary and equip restrictions, including dead/respawn state. | `multiplayerPanelKey`, hidden-panel deferral and dirty rendering. **Quiet regression risk:** add HUB restriction state to the cached inventory key, otherwise an open panel may retain enabled equip buttons after entering the hub. |

Other common paths: `HANDOFF.md`, `docs/qa/social-cargo-integration.md`, `package.json`, `src/multiplayer/protocol.js`, `tests/sbu-cargo.test.js`, `tests/server-cargo.test.js`. Preserve both scripts/test groups and delivery claims. The tractor delivery also carries checked performance/wildlife changes in `navigation`, `character`, `equipment`, `remote-players`, `social-ui`, EVA and fauna modules; rolling back to HUB's older versions would lose that delivery even where HUB itself made no edit.

## Wire and durable-state union

- Existing shared state: `stationFrame:{direction,orientation,altitude}`, `players`, `doors`; recipient state: `hangar`, `inventory`, `commerce`, `health`, nearby `drops`.
- HUB adds `hub:{frame,transit,handsFree,elevators}`. Transit is `{from,to,phase,progress}`; elevator entries are `{frame,open,progress}`. Preserve `station:hub` versus `hangar:N`, authoritative passenger progress, and the distinction between occupied frame and parked berth. `defense` contains `{mountId,yaw,pitch,recoil}` per mount. Forward `stationHub` events (`destinations`, `equipmentRetail`) to station services and `stationStrike` events to the renderer.
- HUB commerce adds `markets` and `marketTerminals`; tractor commerce adds nearby `loose` entries with crate identity/resource/SBU, world `position/quaternion`, and `holder/until/movedAt` lease state. Requests use `action:'cargo'` and `op:'tractor-grab'|'tractor-release'|'tractor-align'|'tractor-move'|'tractor-stow'`; passenger requests use `action:'stationHub',destination:'hub'|berthNumber`.
- Preserve `commandId` replay receipts, revision validation, serialized SQL transactions, market stock bounds and trusted terminal/ship context. Normalize old ledgers exactly once without refilling an existing market. No SQL reset/import is needed by these two deliveries; retain the existing store and migrations. The paired frontend/API restart must carry the same combined contract.
- Fleet's protocol5 must additionally carry its agreed new Atlas mechanism state. Current `playerSnapshot().freighter`, `applyAuthoritativePeer`, remote ship presentation and `server/room.setHull()` still target the existing freighter systems; merely switching the rendered GLB to64m would not carry new ramps/gear/lifts or collision dimensions.

## New station bay / Atlas resize: preserve local human frames

The fleet claim proposes84m clear length and about21.9m height, floorY−8 and back wallZ+26 fixed, preserving human props/terminals/lifts and all20 berth IDs. That geometry is future/unaudited here. Do not scale the complete bay group: it would also scale passenger cabins, terminal reach and authored collision props.

Current berth lift is created at **localZ22.3**, `floor=pod.interiorBox.min.y`; hub lift is **localZ14.3**. `elevatorLocation()` uses the frame's inverse world transform and `lift.z/floor`, with human dimensions: cabin absX<1.65, relativeZ0.65..3.1, eye within0.5m of floor+eyeHeight. Destination placement is `frame.toWorld([0,lift.floor+eyeHeight,lift.z+1.65])`. Keep these definitions coherent in `station-complex`, `station-services`, `station-hub-policy`, `server/station-hub` and server-installed elevator collision boxes. HUB bounds remain local **[-22,-8,-19]..[22,1.5,19]**. Do not derive membership from selected berth or a client label.

The22 registered commodity terminals retain their world conversion: berth local **[-12,floor+1.75,20.7]**, hub screens **[−5.5/5.5,−6.59,−11.792]**, reach2.8m. If any prop moves, update the common terminal definition and real mesh together. Fleet must recompute actual `interiorBox`, `openingZ`, door sweep, approach/pad transforms and server/client hull collisions; keep the parked ship at its leased berth while a pilot transfers between passenger frames.

Bastion08 placement pins station exterior SHA **`5b39b183030d529a710de0b2dad308a36a8e597b067d3061d82f01bb721b76e6`**, current assembled bay/hub/prop geometry and mount roots X±665/Z0, upperY−17.997183184697576 or lowerY−92.0070038910506 with lower quaternion[0,0,1,0]. The whole turret's full-motion bound is R29.6/Y0..38.1 in each mount's frame. Resizing the bays invalidates the surrounding-station portion of that certificate even if the exterior GLB and mounts stay unchanged. Re-run `assets/station-defense/check_placement.mjs` on the final assembled client/server station and include expanded bay/door/approach volumes. The previous closest upper structural margin6.411933m is a receipt for the old assembly, not a promise for the refit. Firing arcs were explicitly obstructed in the earlier audit; a placement pass must not be called clear firing arcs or change the user-authorized defense authority policy.

## Steward's bounded verification

After semantic integration, run the combined HUB/security/ramming/market, tractor/cargo/real-SQL, and PERF UI/snapshot tests. Add focused combined checks for market stock plus loose-crate persistence/replay; recipient-private hub/commerce snapshots; held tractor/tool restriction through passenger transfer and neutral rearm; and dirty inventory restrictions while the panel stays open. Then run paired-client physical hub/tractor/defense smoke and the final resized station/Atlas geometry checks in the existing serialized GPU queue. Existing branch passes do not certify this union. This map is source analysis, not a merge, compatibility test, new art review or delivery approval.
