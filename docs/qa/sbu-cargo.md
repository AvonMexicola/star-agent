# SBU cargo verification

Implementation checkpoint, not independent art or release acceptance.

CPU evidence so far:

-718full unit tests pass;11packing/model checks cover1–64SBU volume, nominal8/512
 capacities, full-grid fit, stacking support, clearance, custody and receipts.
-Local packing persists the ore deduction and crate together. Failed writes and
 stale tabs leave original cargo intact; no loss or duplicate reward.
-Six server cargo checks pass: authoritative reach/docking,1SBU carry/theft,
 failed writes, loose-resource packing/replay, Atlas selection, isolated PostgreSQL
 migration/rollback/reconnect, selling after the base owner disconnects, and a
 continuous standard-input walk up another pilot’s open Nomad ramp. The closed
 hatch blocks entry. Fixture placement is explicit in these CPU service checks;
 they are not a rendered multi-player journey.
-The separate commonPyre outcrop test passes exact server carving/yield and denial
 of forged range/fire. Production build passes with the existing Vite chunk warning.
-Atlas512individual1SBU crates fit in296ms total in the packing diagnostic. Renderer
 batches materials/labels and uses authored low detail for all but24nearby crates
 in dense manifests. No FPS acceptance is inferred from a CPU diagnostic.

Authoring: original Blender5.2 reproducible assets/cargo/build.py and freight-kit.blend.
Multiplayer suite:95pass,1pre-existing skipped fixture,0fail (96total).

Detailed crates2592–3888triangles,186–279kB each; actual maximum bounds fit nominal
cell slots, including handles/latches. A64SBU container occupies1.2×2.4×4.8m of grid.
Cargo support is reserved by nominal bounds with millimetre visual clearances.
The8SBU Nomad grid is starboard aft of the sample locker; existing port rack,
berth and centreline are retained. The playable30m Atlas has512SBU along two fixed
side decks; its8×10m belly lift and side-lift machinery are excluded. The separate
64m AtlasMarkII studio needs a different measured grid when it becomes playable.

First browser checkpoint: the controller-only station journey passed in1.1minutes:
stand, open hatch, walk to terminal, buy four crates, overflow rejection, desktop
and390×844phone manifest, heldRT suppression, walk back, carry and stow. No captured
console/page errors. This was the earlier4SBU grid. Atlas physically reached its
cargo deck, then its preloaded manifest fixture timed out because the dev start
uses a separate in-memory save. The explicit cargo-test fixture now seeds that
isolated store; normal practice and online saves remain separate.

Mesh inspection then established room for four vertical Nomad layers, raising its
capacity to8SBU. The reproducible authored-GLB triangle test checks all eight crate
bounds against the current Nomad hull/furniture: zero intersections. Actual crate
top is3.35m, below the3.39m procedural ceiling. Grid footprint is0.6×1.2m, starting
at ship-local[.94,1,2.16]; the central walking aisle is retained. The same triangle audit found Atlas deck-tread and aft-beam intersections in the
first layout. Raising its grid base to4.025m and moving its start toZ=.1m removes
all authored mesh intersections at full eight64SBU capacity. The updated browser
journey and Atlas render are pending the next serialized GPU window. Aisle-facing
manifest labels were added after inspecting the first capture.

Physical-controller, rendered multi-player journey, independent visual acceptance
and production release are not claimed. Shared preview/database are unchanged by
this branch at this checkpoint.
