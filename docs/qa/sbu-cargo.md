# SBU cargo verification

Implementation checkpoint, not independent art or release acceptance.

CPU evidence so far:

-716full unit tests pass;9packing/model checks cover1–64SBU volume, nominal4/512
 capacities, full-grid fit, stacking support, clearance, custody and receipts.
-Local packing persists the ore deduction and crate together. Failed writes and
 stale tabs leave original cargo intact; no loss or duplicate reward.
-Six server cargo checks pass: authoritative reach/docking,1SBU carry/theft,
 failed writes, loose-resource packing/replay, Atlas selection, isolated PostgreSQL
 migration/rollback/reconnect, selling after the base owner disconnects, and a
 continuous standard-input walk up another pilot’s open Nomad ramp. The closed
 hatch blocks entry. Fixture placement is explicit in these CPU service checks;
 they are not a rendered multi-player journey.
-The separate commonPyre outcrop test passes exact server carving/yield and denial
 of forged range/fire. Production build passes with the existing Vite chunk warning.
-Atlas512individual1SBU crates fit in296ms total in the packing diagnostic. Renderer
 batches materials/labels and uses authored low detail for all but24nearby crates
 in dense manifests. No FPS acceptance is inferred from a CPU diagnostic.

Authoring: original Blender5.2 reproducible assets/cargo/build.py and freight-kit.blend.
Detailed crates2592–3888triangles,186–279kB each; actual maximum bounds fit nominal
cell slots, including handles/latches. A64SBU container occupies1.2×2.4×4.8m of grid.
Cargo support is reserved by nominal bounds with millimetre visual clearances.
The4SBU Nomad grid is starboard aft of the sample locker; existing port rack,
berth and centreline are retained. The playable30m Atlas has512SBU along two fixed
side decks; its8×10m belly lift and side-lift machinery are excluded. The separate
64m AtlasMarkII studio needs a different measured grid when it becomes playable.

Browser/controller/phone evidence is pending the reserved5535 window. No browser,
physical-controller, independent visual acceptance, or production release is claimed
at this checkpoint. Shared preview/database have not been modified.
