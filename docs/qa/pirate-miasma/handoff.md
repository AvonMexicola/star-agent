# SA-PIRATE-003 — checked offer, 2026-09-09

Status: validated development checkpoint offered to parent integration. Parent
owns final shared merge/export after its complete appearance journey. This owner
has not merged shared source, refreshed shared services, pushed a branch or
created a new PR. Physical-device, independent art and performance acceptance
remain pending.

Branch `feat/pirate-miasma`, isolated `.worktrees/pirate-miasma`, based on checked
`cc1e74933791a3abdf36e65bdad732b4985994a9`. Checked final runtime `cc6b035`; full
controller source `fabe36b`. Parent appearance `48be92e` was privately merged in
`63164c5`. Parent private `65814dc` already contains `cc6b035`, stricter identifier
validation and the newly registered scenery test. Delivery changes above
`cc6b035` are curated evidence, task status and documentation only.

## Result and boundaries

Veil Exchange adds an independent solo pirate market in Miasma's Vitriol Basin.
Existing Hush retains its Selene site and exact stock identity. Both receive
Crimson structural paint, rigid prints and enclosed two-door vestibules, using
actual existing leaf animation, opposite-door interlock and swept closing safety.
The existing reduced station turret remains on its cylindrical tower. Both sites
have a safe outer landing apron and canonical supported ground approach; Veil's
route was actually traversed with controller input.

Owned runtime changes are `src/pirate-compound/*`, the additive dev launcher entry,
the two pirate lookup hunks in `src/settlements/catalog.js`, and the coordinated
main launcher-ID branch. Later approved scenery integration adds
`src/scenery-clearance.js`, optional arguments in `src/miasma-flora.js`,
`src/mineral-fragments.js` and `src/miasma.js`, and one main Miasma update argument.
Tests: existing pirate file plus new `tests/scenery-clearance.test.js`. Fixtures:
shared pirate helpers, retained Hush spec, new Miasma spec/config. No unchanged
asset buffers, terrain sampler, damage authority, protocol or server schema were
modified. No ground NPC owner files were taken over.

## Validation and retained evidence

The [production record](README.md) and [curated receipt](evidence.json) contain
exact commands/source hashes, input/environment, result boundaries, before/after
images and limitations. Raw evidence is in ignored `test-results/miasma-01` and
`miasma-02`; unit and signature receipts are also retained there.

- Full physical Miasma controller route: pass 6.1 minutes, including blocked door
  closing, opposite door interlock, trade/cargo/sale, native focus/held/device
  gates and both-door return.
- Final focused batch: all 3 pass, 5.1 minutes. Native Hush phone/keyboard52.6 s;
  both pirate art/actual Miasma damage-retreat2.8 min; Verdigris art1.3 min.
  Application errors/warnings empty; source unchanged in both browser jobs.
- Four scenery invariants and eighteen pirate invariants, all focused files pass.
  Parent final private composition: all161 normal files65.57 s, build12.57 s,
  repo/plan/diff pass. Our final build6.05 s and repo/plan/diff pass.
- Parent reviewed final author images and found phone overlap and scenery
  intrusion resolved, signs/roofs readable. No formal independent art score.
- Retained first-art failure: duplicate non-configurable injected getGamepads;
  fixed in fixture. Retained real before images: phone action over movement,
  flora through Veil. Retained sandbox Vite-cache EROFS followed by authorized
  standalone build pass; no application workaround or new permission prompt.

## Integration contract

Preserve garage action/hint installation **before** pirate `attachInteractions()`.
Normal public settlement records/operators/count and online enabled predicate
remain intact; pirate lookup uses `pirateMarketById`. Market version2 only adds
missing Veil once and retains Hush stock. Discovery and isolation remain
session-only independently per site, as explicitly approved by parent.

The additive scenery API is:
`Miasma.update(position, origin, elapsed, shipPosition, constructionClaims=[])`.
Main supplies `[...build.claims,...settlements.claims]` filtered to body `miasma`,
covering player construction, ordinary Verdigris and both relevant pirate claims.
The claims and other coordinates must share Miasma's canonical body frame. If
later composing World004 rotation, preserve its `miasmaOrigin` and transformed
ship arguments and supply equivalently transformed claim frames; do not silently
mix rotated navigation frames with canonical scenery candidates. No pending
World004 source was copied here.

Register `tests/scenery-clearance.test.js` in the normal test inventory; parent
`65814dc` already does so. The parent's later strict paint/print identifier guards
are retained by its composition and do not change valid-string rendered content.
No shared local-development document was edited from this feature lane; parent
should add both Hush/Veil launcher entries and the player-guide link when recording
the actual integrated/served receipt.

Both private5686/API8686 browser jobs exited and their guard released. No owned
preview/browser process is left running. Existing approved guard path remains
under the earlier pirate worktree's ignored test-results, with bounded miasma-NN
mode; it checks actual host executable/argv immediately before launch. No further
GPU job is requested by this owner.
