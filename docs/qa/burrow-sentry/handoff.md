# SA-VEH-003 — Burrow Sentry checked offer

Implemented and author-validated; parent independently reviewed source and the
corrected final evidence. **Parent owns local integration, the protocol-10
paired preview refresh and PR106.** No shared service or public deployment was
changed by this feature worktree.

The application is **6334b98**, preserving checked shared **1da9b0e** and parent
composition **e9ea4ab**. QA head **c31d9ee** changes only evidence analysis/tests
above the frozen **d051d9b** browser run. The branch is `feat/burrow-sentry`,
worktree `.worktrees/burrow-sentry`. All owned private 5678/API8678 processes are
closed. The tracked HANDOFF matches the checked ancestor; live coordination is
append-only in the root journal.

## Delivered behavior

A separate Meridian Shipworks S-04 replaces Burrow’s rear mining bin with a
sealed operator pod and twin laser turret. Normal mining Burrow stays intact.
The pilot drives; the rear occupant owns aim/fire, with neutral-gated pilot
fallback after exit. Physical doors, steps and both character poses accompany
server-owned seats, motion, fire and 180 HP hull damage. Clear glazing and flat
LCARS controls contain no central divider. Real barrel sockets originate shots.

Canonical rover drive/support and full-envelope Atlas ramp/deck/frame carry
remain shared. Gannet safely rejects the taller turret. All-crew station
friendship/security, held-input gating, empty orphan cleanup and rotating-frame
collision/ray/seat conversion are covered. See [controls](../../burrow-sentry.md)
and [authority](../../decisions/sentry-authority.md).

Owner modules are `src/sentry/`, `server/sentry.js`, Blender source/export,
focused tests and portable QA. Declared narrow adapters compose existing room,
combat/security, character/input, carrier/support, launcher/menu/fauna/main hooks.
Parent owns socket diagnostics, the cross-frame cargo helper, shared conflict
resolution and final delivery metadata.

## Evidence and exact limit of acceptance

- Parent exact 6334 checks: **1,256 normal / 164 files**, 62.895 s; **226
  multiplayer + 2 existing opt-in skips**, 63.211 s; repo 336/plan pass.
- Owner: carrier 08 **7/7**, actual-room cross-frame hits 07 **6/6**, and both-seat
  per-tick/delayed-snapshot/invalid-jump continuity 14 **2/2**, 2.103 s.
- Guarded attempt 12 builds the actual flagged bundle in **4.21 s**,
  `main-C7yzYpq9`; source and all JS/CSS hashes retained.
- Complete solo controller **PASS 1.5 min** and keyboard/native 390×844
  **PASS 1.6 min**, including both solo seats, drive/reverse/fire/inventory/return,
  real focus loss, held modal/device transitions and reachable phone walking.
- Two-account gameplay completed all physical station/EVA, both-seat,
  drive/gunner-fire/fallback, controller and panel server-inventory and final
  exit assertions in **5.0 min**, both connections true and both riders unseated.

The original 12 runner is **exit 1**: its deferred fixed-distance sampling check
reported a 0.7282097 m gunner step during a 668.5 ms render gap. The same received
pose had been held 976.2 ms. The checked time-aware analysis uses the authored
0.85 m/s speed, one 15 Hz publication interval and existing 4 mm tolerance; both
original traces pass. Actual-room tests reject identical movement in 20 ms.
Parent independently replayed the exact input/checker hashes and accepted this
corrected analysis of the completed journey. **No unchanged browser journey was
rerun and the original failure remains unchanged.** Full derivation, source
hashes and curated original screenshots are in [verification](README.md).

Raw attempt: `test-results/sentry-2026-09-09T01-09-55.235Z`; reanalysis:
`assets/burrow-sentry/.staging/access-reanalysis12-02.json`. Original source hashes
are unchanged; private-process cleanup has no survivors. Chromium 151.0.7922.173
used ANGLE/AMD Radeon 860M/OpenGL ES 3.2. Errors/warnings are empty; online retains
two aborted music requests and context-close Vite reset. No online focus
emulation was needed. The extra pointer panel check is disclosed separately
from the injected-controller route; no physical hardware/FPS claim is made.

The 09 boarding socket rejection remains **unresolved, not reproduced in 12**.
Fixed-second outbound peaks were 23/24; neither this pass nor bounded CPU probes
establishes 09’s cause. No server limit changed. Every original failure, including
the first evidence-checker corrections, remains in [the ledger](failures.md).

GLB SHA-256 is
`db8b8d07afd7ad8507b746eefc077553f6996e6189cf623d2dbe60ca7ac48387`,
2,469,344 bytes / 25,998 triangles. Source, maps and reproducible Blender commands remain
in [asset provenance](../../../assets/burrow-sentry/README.md). No model bytes
changed during the final input/frame/evidence corrections.

## Parent integration

Draft [PR106](https://github.com/AvonMexicola/star-agent/pull/106) stacks on the
checked dependency chain. All five hosted checks passed metadata 40677af /
runtime 6334, run 34296422473. Parent has separately composed later checked
Recovery source/records at **b46eda1**, runtime composition **bdc4bd5**, with
1274 normal cases, build and 226 multiplayer/two opt-in skips passing. Those
composition receipts are parent-owned; the feature browser evidence stays
labeled 6334. Parent consumes this metadata offer, updates local guide/task/
review, preserves the shared journal and uses the existing managed service for
a paired browser/API refresh. No schema or persistent fleet is added.

Scope limits: no economy, pressure simulation, hand IK, Gannet fit, general
construction/garage deployment, pending Pirate enemy adapter, new authoritative
ship-gun or full ship-versus-rover ram model. Physical devices, independent
scored art review and performance acceptance remain separate.
