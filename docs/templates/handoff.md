# <task ID> — handoff at <timestamp>

Status and next owner:
Branch/base/current SHA; PR and dependency heads:
Owned files/worktree/ports/processes (no secrets):
Player-visible result and exact scope:

## What changed and what remains

Implemented behavior; incomplete paths; pending source/art/server work; no inferred approvals.

## Validation

For each check: command, tested commit/asset, actual result, evidence location and
limitation. Record failed attempts and corrections. Browser/backend/resolution/scale,
seed and physical versus injected input where relevant.

## Integration and operations

Conflicts resolved/pending, integration SHA, preview URL and actual release status.
Database/schema/generator/protocol compatibility. Owned services left running and
how the authorized next owner can stop/restart them. Do not expose credentials.

## Resume here

First concrete action, blocker/reproduction, coordination needed and who has authority.
Mark a task transferred only when the owner yields or Cees reassigns it.
