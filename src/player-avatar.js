/** The offline player asset and its matching attachment calibration. */
export const PLAYER_AVATAR = Object.freeze({
  url: '/models/props/player-expedition.glb',
  rig: 'player-expedition',
  modelYaw: Math.PI,
});
